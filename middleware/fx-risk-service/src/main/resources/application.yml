
spring:
  kafka:
    bootstrap-servers: localhost:9092
server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: prometheus
  endpoint:
    prometheus:
      enabled: true