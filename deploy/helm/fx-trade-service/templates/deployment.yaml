apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "fx-trade-service.fullname" . }}
  labels: {{- include "fx-trade-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels: {{- include "fx-trade-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "fx-trade-service.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: api
    spec:
      serviceAccountName: {{ include "fx-trade-service.serviceAccountName" . }}
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets: {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: app
          image: {{ include "fx-trade-service.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with (include "fx-trade-service.profiles" .) }}
          args:
            - "--spring.profiles.active={{ . }}"
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          env:
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: {{ .Values.kafka.bootstrapServers | quote }}
            - name: FX_OPENSEARCH_SOURCE_TYPE
              value: {{ .Values.opensearch.source.type | quote }}
            - name: FX_OPENSEARCH_SOURCE_MASTERDATA_URL
              value: {{ .Values.opensearch.source.masterdataUrl | quote }}
            - name: FX_OPENSEARCH_SOURCE_TTL_SECONDS
              value: {{ .Values.opensearch.source.ttlSeconds | quote }}
            - name: AWS_REGION
              value: {{ .Values.aws.region | quote }}
            {{- if .Values.aws.credentialsSecretName }}
            - name: FX_AWS_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.aws.credentialsSecretName }}
                  key: access-key
            - name: FX_AWS_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.aws.credentialsSecretName }}
                  key: secret-key
            {{- end }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
          resources: {{- toYaml .Values.resources | nindent 12 }}
