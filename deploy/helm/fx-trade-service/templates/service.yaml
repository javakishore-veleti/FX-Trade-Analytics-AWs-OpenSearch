apiVersion: v1
kind: Service
metadata:
  name: {{ include "fx-trade-service.fullname" . }}
  labels: {{- include "fx-trade-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "fx-trade-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
