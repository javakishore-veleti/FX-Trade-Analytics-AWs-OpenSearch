apiVersion: v1
kind: Service
metadata:
  name: {{ include "fx-masterdata.fullname" . }}
  labels: {{- include "fx-masterdata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "fx-masterdata.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
